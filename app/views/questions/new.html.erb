<div class="container">

	<h2><center>New Question</center></h2>
	<div class="row">
	  <div class="span6 offset3">
	    <%= form_for(@question) do |f| %>
 

	      <%= f.label :title, "Question title" %>
	      <%= f.text_field :title %>

	      <%= f.label :content, "Question content" %>
	      <%= f.text_area :content %>

	      <%= f.hidden_field :course_id, :value => @course.id %>



	      <p><h1> </h1></p>

	      <%= f.submit "Create question", class: "btn btn-large btn-primary" %>
	    <% end %>
	  </div>
	</div>
</div>

