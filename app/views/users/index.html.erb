<% provide(:title, 'All users') %>

<div class="container">
	
	<div class="row">
		<div class="span6">
			<h1><center>All users</center></h1>
			<%= will_paginate %>

			<ul class="users">
				<%= render @users %>
			</ul>

			<%= will_paginate %>
		</div>
		<div class="span4">

		</div>
	</div>

</div>