<% provide(:title, 'All users') %>

<div class="container">
	<h1><center>All users</center></a></h1>

	<%= will_paginate %>

	<ul class="users">
		<% # @users.each do |user| %>
			<%= render @users %>
		<% # end %>
	</ul>

	<%= will_paginate %>

</div>